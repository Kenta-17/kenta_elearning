<div class="container">
    <div class="row">
        <div class="col-md-7 mx-auto">
            
                <div class="row">
                    <div class="col-md-9">
                        <h2><%= @category.title %></h2>
                    </div>
                    <div class="col-md-3 my-auto text-right">
                        <%= @lesson.words.count + 1 %> of <%= @category.words.count %>
                    </div>
                </div>

                <div class="jumbotron text-center">
                    <h2><%= @lesson.next_word.content %></h2>
                 
                    <% @lesson.next_word.choices.each do |choice| %>
                        <%= form_for([@lesson, @answer]) do |f| %>
                            <div class="form-group¥">
                                <%= f.hidden_field :word_id, value: choice.word_id %>
                            </div>
                            <div class="form-group">
                                <%= f.hidden_field :choice_id, value: choice. id %>
                            </div>
                            <div class="form-group mb-2">
                                <%= f.submit choice.content, class: "btn btn-outline-info w-50" %>
                            </div>
                        <% end %>
                    <% end %>
                </div>
        </div>
    </div>
</div>

